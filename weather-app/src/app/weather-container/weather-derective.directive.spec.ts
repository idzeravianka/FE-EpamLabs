import { WeatherDerectiveDirective } from './weather-derective.directive';

describe('WeatherDerectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new WeatherDerectiveDirective();
    expect(directive).toBeTruthy();
  });
});
