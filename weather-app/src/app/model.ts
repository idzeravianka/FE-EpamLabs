export interface CityWeatherData {
  city: string;
  description: string;
  icon: string;
  temperature: number;
}

export interface CurrentCityWeatherData{
  city: string;
  description: string;
  icon: string;
  temperature: number;
}